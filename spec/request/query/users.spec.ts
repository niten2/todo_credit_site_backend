describe("", () => {

  it("should return ", async () => {


    console.log(app)


    // const query = `
    //   query {
    //     users {
    //       name
    //       email
    //     }
    //   }
    // `

    // let res = await request("/v1", {
    //   method: "put",
    //   unauth: true,
    //   params: query,
    // })

    // let res = await grapql({ query })
    // let res = await request({ url: "/v1", params: query, method: "get" })
      // unauth: true
      // params,
    // })

    // console.log(res.body)

    // expect(res.body).toEqual({
    //   name: 'credit_site',
    //   current_version: '/v1'
    // })

  })
})
